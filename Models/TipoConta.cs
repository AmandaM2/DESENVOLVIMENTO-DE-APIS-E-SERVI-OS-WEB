namespace Api.Models
{
    // Enum é uma lista de opções fixas. Ajuda a não escrevermos strings erradas.
    public enum TipoConta
    {
        Corrente = 1,
        Poupanca = 2,
        Empresarial = 3
    }
}
